This is the wiki of this repository.

To build it locally you will need to install mdBook.
https://rust-lang.github.io/mdBook/guide/installation.html

Navigate to the wiki folder with the `book.toml`

`mdbook serve` to open a dev server to see your changes live.
`mdbook build` to build the HTLM view for deployment.
